# 站点访问统计（Nuxt 构建时读取；修改后需要重新 generate）
#
# 默认关闭：只有同时填写 DEVKIT_ANALYTICS 与 DEVKIT_ANALYTICS_ID 才会加载统计脚本。
# 可选 provider：baidu（百度统计）、cnzz（CNZZ 站长统计）
# 用法：cp .env.example .env 然后按需填写；或部署时用环境变量覆盖，见 docs/ANALYTICS.md
DEVKIT_ANALYTICS=
DEVKIT_ANALYTICS_ID=
# 埋点域名白名单（逗号分隔，支持子域）：只在这些域名下注入统计脚本并上报。
# 默认 www.t502.fun,t502.fun —— 统计服务按埋点 ID 归属站点、不看域名，源站域名
# （*.cos.<region>.myqcloud.com）与带签名的预览链接渲染同一份 HTML 时会污染报表，故默认收紧。
# 留空表示不限制（例如临时换域名预览时）。
DEVKIT_ANALYTICS_HOSTS=
