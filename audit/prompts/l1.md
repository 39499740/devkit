你是 DevKit 前端子代理 L1（宽屏留白优化）。

【背景】用户反馈：宽屏上页面左右两侧大量空白。原因是多处容器写死 max-width: 1140px 并居中。

【工作约定】
- 工作目录 /Users/hao/WebstormProjects/web_tools（Nuxt 4 在 devkit/，dev server 在 http://localhost:3000，保存即 HMR）。
- 只允许修改下面列出的文件；不要动 app/components/Dk*.vue（DkEditor/DkCollapse 刚改过）、app/utils/**、app/composables/**、app/components/tools/**。
- 禁止修改 design.pen、docs/**。
- 保持现有视觉语言（app/assets/css/main.css 的令牌与既有间距），中文界面，不要写注释，不新增依赖。
- 必须自测：用 playwright 在 2560 / 1920 / 1440 / 390 四种宽度下量测并截图，把命令与输出写进报告。最后一行输出 DONE: <绝对路径>。

【可改文件】
- devkit/app/components/ToolPageLayout.vue
- devkit/app/pages/index.vue、category/[id].vue、favorites.vue、recent.vue、settings.vue、privacy.vue、help.vue、offline.vue
- devkit/app/error.vue

【策略（必须按此执行，并保持一致性）】
1. 工具页（ToolPageLayout 的 .tool-page）：去掉 max-width 限制，让工作区吃满可用宽度（宽屏下两个编辑器应各占约一半）。保留左侧固定 236px 侧栏与 shell 的 32px 内边距。
2. 卡片网格/列表类系统页（首页 index、分类页 category、我的收藏 favorites、最近使用 recent、偏好设置 settings）：最大宽度放宽到 1680px（居中），保证宽屏下网格能有更多列而不是两侧留白。
3. 阅读类页面（隐私 privacy、帮助 help、离线 offline、错误页 error.vue 的 .err）：最大宽度 1200px（保证行宽可读），但要去掉不必要的窄限（例如 error.vue 现在的 1100 可以保留或统一为 1200）。
4. 任何内部面板不要出现新的固定宽度；表格/网格用 fill 或 auto-fit。
5. 移动端（≤960）与 1440 的现有表现不能变差：不能出现横向溢出。

【验证（必须做）】
- 在 2560×1400 下量测：工具页正文宽度应接近 shell 内容区宽度（减去内边距）；首页/收藏页网格列数应随宽度增加。
- 四个宽度下跑 overflow 检查（documentElement.scrollWidth <= clientWidth + 1），列出任何溢出元素。
- 截图：2560 的工具页（如 /tools/json-format）、首页、收藏页；1440 工具页；390 工具页。
- 报告写 /Users/hao/WebstormProjects/web_tools/audit/reports/l1-wide-layout.md。