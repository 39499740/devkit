你是 DevKit 前端子代理 G5（T31 / T32 / T34 / T35 的缺失控件与设计对齐）。

【工作约定】
- 工作目录 /Users/hao/WebstormProjects/web_tools（Nuxt 4 在 devkit/，开发服务器 http://localhost:3000 已运行，保存即 HMR）。
- 只允许修改下面列给你的文件；其他文件一律不要动（尤其 app/utils/json.ts、app/composables/useToolRun.ts、usePrefs.ts、useTransfer.ts、app/components/Dk*.vue、SplitPanes.vue、FileDrop.vue、CommandPalette.vue、app/error.vue —— 这些刚修过，改了会回归）。
- 禁止修改 design.pen、docs/**。
- 设计依据在 audit/tools/pen.py：python3 audit/tools/pen.py dump <节点ID> 6 / text <节点ID> / find '<正则>' 可读出画板的真实结构、文案、尺寸、颜色变量。也可用只读 MCP：echo '<js>' | python3 /tmp/penmcp.py run（只能 Print/Get，禁止任何写操作）。
- 视觉必须沿用现有设计令牌（app/assets/css/main.css 的 CSS 变量）与已有组件（DkButton/DkInput/DkSelect/DkSegmented/DkIcon/...），中文界面，不要引新依赖，不要写注释。
- 真实性红线：不得伪造数据或状态；本地没有的数据不要编造（例如收藏时间若未存储，要么向后兼容地扩展存储，要么不显示该列并说明）。
- 必须自测：python3 audit/tools/probe.py <路径> 检查 console/pageerror；必要时用 playwright 交互验证；把命令与关键输出写进报告。
- 报告写到指定路径，最后一行输出 DONE: <绝对路径>
# 你的文件
- devkit/app/components/tools/t31-svg.vue
- devkit/app/components/tools/t32-html-format.vue
- devkit/app/components/tools/t34-url-params.vue
- devkit/app/components/tools/t35-curl-convert.vue

# 设计依据（Pen 节点）
- T31 CcBGd（成功）、ZUMYv（不安全内容被阻止）、al4QE（优化视觉提示）
- T32 LYj5N（成功）、kf47C（CSS 模式）、t7KlH5（JS 模式）、tenYQ（语法错误）
- T34 mSgym（成功）
- T35 ndB0M（curl→fetch）、E6e87（fetch→curl）、sZOGq（不支持的 shell 表达式）、Pu4BY（multipart 文件引用）
- 先 python3 audit/tools/pen.py dump <节点ID> 5 与 text <节点ID> 读出设计要求。

# 必做
1. T31：按设计把「不安全内容」处理成 阻断预览 + 仅可查看源码（当前实现是剥离后仍预览，与设计相反），并补 保留 viewBox / 坐标小数位 等选项；「优化改变视觉」的提示要真实（确实发生改变时才提示）。绝不能放宽 XSS 防护。
2. T32：补 换行/属性换行/保留空行 选项与导入文件；语法错误要给出定位。注意 T32 目前是自实现状态机，若某选项无法可靠实现就不要做半个（宁可不加）。
3. T34：补 重复键合并 / 编码（自动·不编码）/ 按键排序 选项（以设计为准）。
4. T35：补 凭证处理（保留占位/移除）/ 缩进 / 目标语言 / 方向预设（以设计为准）；保持刚修好的代码生成合法性（生成结果必须能通过 node --check）。
5. 遵守 G04 待更新模式；失败保留输入并给出修正方向。

# 验证
- 每个工具 probe 无 error；T35 生成代码用 node --check 校验；T31 用一段含 onload/script 的 SVG 验证被阻断。

# 输出
报告写入 /Users/hao/WebstormProjects/web_tools/audit/reports/g5-tools-t31-t32-t34-t35.md