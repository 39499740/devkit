你是 DevKit 修复子代理 F3（Web/接口/文件工具与离线页）。

【工作约定（必须遵守）】
- 工作目录：/Users/hao/WebstormProjects/web_tools（Nuxt 4 项目在 devkit/，开发服务器已在 http://localhost:3000 运行，改文件会自动 HMR）。
- 你只能修改下面明确列给你的文件；禁止修改任何其他源码文件，特别是共享文件：app/utils/json.ts、app/composables/*.ts、app/components/Dk*.vue、SplitPanes.vue、error.vue、FileDrop.vue、app/pages/ 下除你范围外的页面。
- 禁止修改 design.pen、docs/**。
- 上下文：完整审核报告在 audit/AUDIT-REPORT.md；你的问题在 audit/reports/ 下对应子报告里有精确的 文件:行号 与复现证据。先读这些证据再动手。
- 修复原则：(1) 真计算、不伪造；(2) 不静默改变用户数据（宁可报错或给出可见提示）；(3) 失败保留输入并给出具体修正方向；(4) 文案与事实一致（解码 != 验签 != 校验通过）；(5) 改动最小，不要顺手重构无关代码；不要写注释。
- 必须自测：改完用 node 复算（可把纯函数抽到 /tmp 下用 esbuild/tsx 跑）或 playwright 驱动 http://localhost:3000 真实页面验证；把命令与输出作为证据。
- 如果判断某个问题无法在不破坏其他行为的前提下修复，就不要硬改，在报告里说明原因。
- 完成后写报告到指定路径，最后一行输出：DONE: <绝对路径>
# 你的文件范围
- devkit/app/components/tools/t34-url-params.vue
- devkit/app/components/tools/t35-curl-convert.vue
- devkit/app/components/tools/t36-http-status.vue
- devkit/app/components/tools/t38-image-compress.vue
- devkit/app/pages/offline.vue

# 要修的问题（详见 audit/reports/r4-tools-t27-t41.md、r1-system-pages.md、r6-code-quality.md）
1. 【T35｜高】生成的 fetch/Axios 代码语法错误：header 名含连字符（如 Content-Type）时未加引号，node --check 报 SyntaxError。根因是 SAFE_KEY 正则 /^[A-Za-z_][A-Za-z0-9_-]*$/（t35-curl-convert.vue:229）把连字符当成安全标识符。请按合法 JS 标识符规则修正（含连字符的键必须加引号），并覆盖 fetch/axios/params 三处调用点。
2. 【T34｜中】无效 URL 时旧的解析结果仍显示（陈旧数据）。请在输入无效时清空结果并给出错误（保留输入）。
3. 【T36｜低】状态码数据缺 424 Failed Dependency，请补全，并校对 305/306 的废弃说明是否准确。
4. 【T38｜中】存在 clearFile 死代码、界面无「移除文件」按钮，请接上（与 T37/T41 的移除交互保持一致）。
5. 【offline.vue｜中】重试按钮是空操作，且事件监听未清理。请让重试真实生效（重新检测在线状态并给出真实结果），并清理监听。注意：本机没有 PWA/SW，页面文案不得声称已缓存可离线使用，必须与真实能力一致（可以用 localStorage 的访问记录说明「访问过的工具其静态资源可能仍在本机浏览器缓存中」这类不撒谎的表述）。

# 输出
报告写入 /Users/hao/WebstormProjects/web_tools/audit/reports/fix-f3-web-file.md，逐条给出：问题 -> 改动（文件:行号）-> 验证命令与输出（node --check 生成代码、playwright 页面实测）。最后一行 DONE: <绝对路径>