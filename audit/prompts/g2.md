你是 DevKit 前端子代理 G2（S04 我的收藏 / S05 最近使用 的设计还原）。

【工作约定】
- 工作目录 /Users/hao/WebstormProjects/web_tools（Nuxt 4 在 devkit/，开发服务器 http://localhost:3000 已运行，保存即 HMR）。
- 只允许修改下面列给你的文件；其他文件一律不要动（尤其 app/utils/json.ts、app/composables/useToolRun.ts、usePrefs.ts、useTransfer.ts、app/components/Dk*.vue、SplitPanes.vue、FileDrop.vue、CommandPalette.vue、app/error.vue —— 这些刚修过，改了会回归）。
- 禁止修改 design.pen、docs/**。
- 设计依据在 audit/tools/pen.py：python3 audit/tools/pen.py dump <节点ID> 6 / text <节点ID> / find '<正则>' 可读出画板的真实结构、文案、尺寸、颜色变量。也可用只读 MCP：echo '<js>' | python3 /tmp/penmcp.py run（只能 Print/Get，禁止任何写操作）。
- 视觉必须沿用现有设计令牌（app/assets/css/main.css 的 CSS 变量）与已有组件（DkButton/DkInput/DkSelect/DkSegmented/DkIcon/...），中文界面，不要引新依赖，不要写注释。
- 真实性红线：不得伪造数据或状态；本地没有的数据不要编造（例如收藏时间若未存储，要么向后兼容地扩展存储，要么不显示该列并说明）。
- 必须自测：python3 audit/tools/probe.py <路径> 检查 console/pageerror；必要时用 playwright 交互验证；把命令与关键输出写进报告。
- 报告写到指定路径，最后一行输出 DONE: <绝对路径>
# 你的文件
- devkit/app/pages/favorites.vue
- devkit/app/pages/recent.vue
- devkit/app/composables/useFavorites.ts、app/composables/useRecent.ts（可小幅扩展，必须向后兼容已有 localStorage 数据）

# 设计依据（Pen 节点）
- S04 FdLxl（已收藏）与 vO8fN（空收藏）：工作区表格（列含 工具 / 分类 / 收藏时间 / 操作），支持排序、全部取消收藏、状态栏与页尾说明区/FAQ。
- S05 hVwyK（记录列表）与 G5GfwW（空历史）：按 今天 / 更早 分组，含分类列、单条移除、清空全部、说明区。
- 先 python3 audit/tools/pen.py text FdLxl / dump FdLxl 5 读出真实列名与文案。

# 必做
1. S04 改为表格/工作区形态：列、行操作（打开、取消收藏）、排序（至少按名称/按分类/按收藏时间）、全部取消收藏、状态栏、页尾说明区（含 FAQ 性质的说明，文案与设计一致或等价）。
2. S05 按设计补：今天/更早分组（composable 已有 grouped）、分类列、单条移除、清空全部、说明区。
3. 真实性：收藏若未记录时间，用向后兼容方式扩展存储（老数据无时间时显示占位说明或归入「未知时间」），不要编造时间；最近使用不得展示任何输入正文/密钥/文件名。
4. 空态要真实可达、给出「浏览全部工具」入口。

# 验证
- probe /favorites 与 /recent：无 console error/pageerror；空态与有数据态都真实可复现（可在浏览器里先写入 localStorage 再刷新验证）。

# 输出
报告写入 /Users/hao/WebstormProjects/web_tools/audit/reports/g2-favorites-recent.md