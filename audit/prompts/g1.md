你是 DevKit 前端子代理 G1（S01 首页 / S02 分类页 的设计还原）。

【工作约定】
- 工作目录 /Users/hao/WebstormProjects/web_tools（Nuxt 4 在 devkit/，开发服务器 http://localhost:3000 已运行，保存即 HMR）。
- 只允许修改下面列给你的文件；其他文件一律不要动（尤其 app/utils/json.ts、app/composables/useToolRun.ts、usePrefs.ts、useTransfer.ts、app/components/Dk*.vue、SplitPanes.vue、FileDrop.vue、CommandPalette.vue、app/error.vue —— 这些刚修过，改了会回归）。
- 禁止修改 design.pen、docs/**。
- 设计依据在 audit/tools/pen.py：python3 audit/tools/pen.py dump <节点ID> 6 / text <节点ID> / find '<正则>' 可读出画板的真实结构、文案、尺寸、颜色变量。也可用只读 MCP：echo '<js>' | python3 /tmp/penmcp.py run（只能 Print/Get，禁止任何写操作）。
- 视觉必须沿用现有设计令牌（app/assets/css/main.css 的 CSS 变量）与已有组件（DkButton/DkInput/DkSelect/DkSegmented/DkIcon/...），中文界面，不要引新依赖，不要写注释。
- 真实性红线：不得伪造数据或状态；本地没有的数据不要编造（例如收藏时间若未存储，要么向后兼容地扩展存储，要么不显示该列并说明）。
- 必须自测：python3 audit/tools/probe.py <路径> 检查 console/pageerror；必要时用 playwright 交互验证；把命令与关键输出写进报告。
- 报告写到指定路径，最后一行输出 DONE: <绝对路径>
# 你的文件
- devkit/app/pages/index.vue
- devkit/app/pages/category/[id].vue
- devkit/app/components/ToolCard.vue（仅在确有必要时微调，保持其他使用方不回归）

# 设计依据（Pen 节点）
- S01 主画板 LiwdR（1440×1583）：主内容区依次是 页头 l5nj6（56 高）、搜索卡片 NAoCz（60 高）、快捷与最近 h9cLZ、分类目录 joSqu。先读这些节点拿到真实文案与结构：python3 audit/tools/pen.py dump l5nj6 5 等。
- S01 首次访问变体 J8Yrb；S02 分类页 h1lFMx（摘要与加密）与 CiCUB（Java 开发）。
- 顶部导航与左侧导航是共享组件实例（ref），不要在页面里重建。

# 必做
1. 首页补页头：图标 + 标题（全部工具）+ 一句说明 + 「本地处理 · 输入不上传」徽标，文案与设计一致（设计 S01 页头文案可用 pen.py text l5nj6 读取）。
2. 首页补搜索卡片：与顶部搜索共用同一套搜索结果规则（点击/回车打开命令面板并带上关键词，或就地过滤工具卡片——按设计选一种并实现真实可用，不要做成摆设）。
3. 首页补「快捷与最近」分区：收藏快捷区 + 最近使用（无数据时按设计给出不编造的空态/推荐入口）。
4. 分类页补页头（分类名 + 用途说明 + 工具数量）与当前分类搜索；保证分类切换不丢导航、只高亮当前入口。
5. 保持 41 工具/8 分类的数据完全来自 app/data/tools.ts，不新增硬编码工具。

# 验证
- probe 首页、/category/format、/category/java：无 console error/pageerror；1440 与 390 宽无横向溢出。
- 搜索卡片真实可用（输入关键词后能命中，例如「国密」→ SM2/SM3/SM4）。

# 输出
报告写入 /Users/hao/WebstormProjects/web_tools/audit/reports/g1-home-category.md