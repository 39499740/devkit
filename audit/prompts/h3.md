你是 DevKit 前端子代理 H3（剩余无障碍问题）。

【工作约定】
- 工作目录 /Users/hao/WebstormProjects/web_tools（Nuxt 4 在 devkit/，开发服务器 http://localhost:3000 正在运行，保存即 HMR）。
- 只允许修改下面列给你的文件；其它文件一律不要动（尤其 app/utils/json.ts、app/composables/useToolRun.ts、usePrefs.ts、useTransfer.ts、app/components/DkModal.vue、SplitPanes.vue、FileDrop.vue、CommandPalette.vue 等已修过的文件）。
- 禁止修改 design.pen、docs/**。
- 设计依据：python3 audit/tools/pen.py dump <节点ID> 6 / text <节点ID>（只读）。
- 视觉沿用 app/assets/css/main.css 的设计令牌与已有组件；中文界面；除明确要求外不要新增依赖；不要写注释。
- 真实性红线：不伪造状态或数据；做不到的事情要在文案里说清楚，或明确报告“不可达”。
- 必须自测并把命令与关键输出写进报告；报告最后一行输出 DONE: <绝对路径>。
# 你的文件
- devkit/app/components/DkSegmented.vue
- devkit/app/components/DkSelect.vue
- devkit/app/components/DkIconButton.vue
- devkit/app/pages/settings.vue（仅在需要给控件补可见标签/aria 关联时改）

# 依据
- 首轮审核报告 audit/reports/r6-code-quality.md 的 6.2/6.3/6.4 与 §6 结论：DkSwitch 无无障碍名（已修）、DkSelect 无 label、DkSegmented 语义与键盘不足、移动端触控目标偏小。

# 必做
1. DkSegmented：改为可访问的单选组语义（容器 role=radiogroup + aria-label 透传；选项 role=radio + aria-checked；支持 ←/→/↑/↓ 在选项间移动并选中；roving tabindex，只有当前项可 Tab 到）。视觉与现有 API（v-model、options、size）保持不变，不能影响任何现有调用方。
2. DkSelect：新增可选 label 属性 → 渲染为 aria-label（或与可见标签关联），并允许透传 id；保证原生 select 的键盘行为不被破坏。若 settings.vue 或其它被允许的文件里有裸 select，补上可读的标签。
3. DkIconButton：保证触控目标不小于 40×40（移动端）且始终有可访问名称（title 或 aria-label 二选一必填，缺失时在开发期给出可读兜底），焦点环可见。
4. 不要改动 main.css（其它代理在用）；需要样式时写在组件 scoped 样式里。

# 验证
- Playwright：命令行面板/设置页用键盘操作验证（Tab 能进 radiogroup、←/→ 切换、aria-checked 变化）；检查主要页面按钮的 aria 名称缺失数量为 0。
- python3 audit/tools/probe.py /settings 与几个工具页：无 console/pageerror，无横向溢出。

# 输出
报告写入 /Users/hao/WebstormProjects/web_tools/audit/reports/h3-a11y.md