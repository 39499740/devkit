你是 DevKit 审核子代理 #7（需求文档覆盖率与设计稿交付完整性）。先读 `/Users/hao/WebstormProjects/web_tools/audit/BRIEF.md`，严格遵守其中约束。

## 你的范围
1) **需求 → 设计稿 → 代码** 的完整覆盖矩阵（文档级，不逐像素）。
2) **设计稿自身交付完整性**（对照任务书第 06/07 节“完成的定义”）。
3) **项目文档与实现一致性**。

## 必须做的事
1. 解析需求文档 `docs/DevKit-Pen完整页面设计任务书.md`：
   - 第 00 节的通用规则（视觉、间距、字号、顶部栏 60、侧栏 220–236、分类 8 个、真实性 10 条、画板命名与变体规则）；
   - 第 01 节 S01–S10 每页要求的要素；
   - 第 02 节 G01–G08；
   - 第 03 节 T01–T41 每个工具的“主画板提示词 / 示例要求 / 必画变体”；
   - 第 04 节 R01–R04；第 05 节 C01–C15 组件与必需变体；第 06 节每批检查；第 07 节最终交付格式与“完成的定义”。
2. 对每条要求，在设计稿中定位对应画板（`python3 audit/tools/pen.py find "<关键词>"`、`dump <id>`、`text <id>`；批次根画板：A1=`cF2g8`、A2=`k0mUFc`、A3=`I5ukDe`、A4=`i8zQUL`、A5=`y4FGt`、A6=`p vxMC`/`lhx49`/`mwcT7`——注意 A6 工具批是 `pvxMC`），判定：
   - 是否存在；
   - 是否只画了主画板而缺“必画变体”；
   - 是否为空壳/占位（无文字、无实际内容、Generating/placeholder 残留）；
   - 是否有溢出/截断/重叠（可用 `pen.py dump <id> 2` 看尺寸，也可用 MCP `Get(id, n=>c.problems && Print(...))` 只读查询检测 `partially clipped / fully clipped`）。
   产出一张**完整清单表**：`编号 | 名称 | 设计稿状态(主画板/变体完成度) | 代码状态 | 结论`。
3. 统计并给出数字：41 个工具主画板是否齐全；必画变体总数 vs 实际画板数；S01–S10；G01–G08；R01–R04；C01–C15。给出“未完成/缺失”的准确编号。
4. 对照代码：`devkit/app/data/tools.ts` 41 条与设计稿 T01–T41 名称/分类是否一一对应（特别检查：设计稿名称与代码 `name` 的用词差异，如“JSON格式化”vs“JSON 格式化”、T27“JSON转TS类型”vs“JSON 转 TypeScript 类型”）；分类归属是否一致；`alias` 是否覆盖设计稿要求的中英文/缩写/别名搜索（例如“国密”能搜到 SM2/SM3/SM4）。
5. 项目文档一致性：`devkit/README.md`、`devkit/FOUNDATION.md`、`docs/` 下历史报告中的断言是否与当前代码/设计稿矛盾（历史报告可能过时，要标出哪些结论已不成立）。
6. 特别核对历史文档 `docs/DevKit-Pen重复设计检查.md` 提出的遗留问题是否已修复：R01/R03 适配稿是否仍沿用旧版 24 工具首页；13 张旧稿是否仍以正式稿形式存在；T12/T37、T05/T41 重叠是否已处理。

## 输出
写入 `/Users/hao/WebstormProjects/web_tools/audit/reports/r7-docs-coverage.md`：包含完整覆盖矩阵（大表，允许长）、缺失编号清单、文档矛盾清单、以及“设计稿交付完成度”统计数字。最后一行 `DONE: <绝对路径>`。
