你是 DevKit 修复子代理 F2（计算与解析正确性）。

【工作约定（必须遵守）】
- 工作目录：/Users/hao/WebstormProjects/web_tools（Nuxt 4 项目在 devkit/，开发服务器已在 http://localhost:3000 运行，改文件会自动 HMR）。
- 你只能修改下面明确列给你的文件；禁止修改任何其他源码文件，特别是共享文件：app/utils/json.ts、app/composables/*.ts、app/components/Dk*.vue、SplitPanes.vue、error.vue、FileDrop.vue、app/pages/ 下除你范围外的页面。
- 禁止修改 design.pen、docs/**。
- 上下文：完整审核报告在 audit/AUDIT-REPORT.md；你的问题在 audit/reports/ 下对应子报告里有精确的 文件:行号 与复现证据。先读这些证据再动手。
- 修复原则：(1) 真计算、不伪造；(2) 不静默改变用户数据（宁可报错或给出可见提示）；(3) 失败保留输入并给出具体修正方向；(4) 文案与事实一致（解码 != 验签 != 校验通过）；(5) 改动最小，不要顺手重构无关代码；不要写注释。
- 必须自测：改完用 node 复算（可把纯函数抽到 /tmp 下用 esbuild/tsx 跑）或 playwright 驱动 http://localhost:3000 真实页面验证；把命令与输出作为证据。
- 如果判断某个问题无法在不破坏其他行为的前提下修复，就不要硬改，在报告里说明原因。
- 完成后写报告到指定路径，最后一行输出：DONE: <绝对路径>
# 你的文件范围
- devkit/app/components/tools/t03-json-yaml.vue
- devkit/app/components/tools/t20-cron.vue
- devkit/app/components/tools/t21-date-diff.vue
- devkit/app/components/tools/t24-properties-yaml.vue
- devkit/app/components/tools/t26-maven-gradle.vue

# 要修的问题（详见 audit/reports/r3-tools-t15-t26.md、r2-tools-t01-t14.md）
1. 【T03｜高】YAML 转 JSON 静默丢精度：yaml.load 使用 JSON_SCHEMA，把 1234567890123456789 变成 1234567890123456800、把 007 变成 7，而页面文案却承诺 007 等不会被自动转成数字。请让转换不再静默改数据：要么把这些标量按字符串保留，要么明确报错并给出可见提示让用户选择。不允许继续静默变形。
2. 【T20｜高】周/月英文名 WED、JUL 等被误判为 Quartz 扩展符（t20-cron.vue:114-122 附近）。请修正名称解析优先级。注意：T20 页面刚被修复过一个崩溃（nextRuns 里的 get('year') 空引用），不要再破坏它。
3. 【T21｜中】日历差在起始为月末时输出负数日（t21-date-diff.vue:129-144）。请修正日历年月日差算法，并保证起止交换、跨 DST 仍正确。
4. 【T24｜中】unicode 解码计数永远为 0（unicodeDecoded 与 res.uCount 字段名不一致，t24-properties-yaml.vue:26/49/70/95）；且 Properties 值尾部空白被 trim 吃掉。请修正，并保证已解码 N 处真实显示。
5. 【T26｜高】classifier 顺序双向颠倒（t26-maven-gradle.vue:89-97,166-171）：Maven 转 Gradle 应为 g:a:version:classifier，Gradle 转 Maven 解析 g:a:1.0:sources 应为 version=1.0 / classifier=sources。另修混排时不支持的 Gradle 声明被静默丢弃的问题：必须给出可见提示。

# 输出
报告写入 /Users/hao/WebstormProjects/web_tools/audit/reports/fix-f2-compute.md，逐条给出：问题 -> 改动（文件:行号）-> 验证命令与输出（node 复算向量 + playwright 页面实测）。最后一行 DONE: <绝对路径>