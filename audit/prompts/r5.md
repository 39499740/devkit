你是 DevKit 审核子代理 #5（共享交互 G01–G08 与可复用组件 C01–C15）。先读 `/Users/hao/WebstormProjects/web_tools/audit/BRIEF.md`，严格遵守其中约束。

## 你的范围
1) 设计稿共享状态/交互 **G01–G08**（批次 A4 的 `i8zQUL`，也可 `python3 audit/tools/pen.py find "^G0"`）：
   - G01 发送到另一个工具（目标菜单 / 替换确认 / 已进入目标）
   - G02 文件处理过程（拖入高亮 / 读取中 / 计算进度 / 取消完成 / 读取失败 / 超出当前限制）
   - G03 剪贴板与下载反馈（复制成功 / 剪贴板不可用 / 下载准备 / 下载失败）
   - G04 结果待更新（编辑前 / 结果已旧）
   - G05 首次进入与示例（空输入 / 示例入口 / 载入替换确认）
   - G06 执行失败与重试（算法不支持 / 解析失败 / 任务取消）
   - G07 长内容工作台（横向滚动 / 自动换行与查找）
   - G08 危险渲染内容预览（源码与隔离预览 / 安全提示）
2) 设计稿组件 **C01–C15**（`pen.py find "^C0"`、`find "^C1"`，以及根级 `组件 · *` 画板：`PQJ6y` 顶部导航、`vxMha` 左侧导航、`I1Bhp` 工具卡片、`o7OL6` 命令面板、`Pet6k` 按钮、`x1e6C` 字段、`tPt2l` 分段、`WJkbJ` 编辑器、`yvE1y` 状态栏、`kWQyu` 文件项、`R4IbS7` 提示条、`CerYs` 弹层、`MBSI5` 树行、`M5DyY` 折叠区、`LYFe2` 变量板）与其代码实现对照。

实现侧：`devkit/app/components/` 下的 `DkButton/DkIconButton/DkInput/DkField/DkSelect/DkSegmented/DkCheckbox/DkEditor/DkCollapse/DkModal/DkStatusBar/DkIcon/DkToastHost/FileDrop/SplitPanes/JsonTree/SendToMenu/CommandPalette/TopNav/SideNav/ToolCard/ToolPageLayout`，以及 `composables/useToolRun.ts`、`useToast.ts`、`useClipboard.ts`、`usePrefs.ts`、`useFavorites.ts`、`useRecent.ts`、`usePalette.ts`、`useTransfer.ts`、`assets/css/main.css` 的 CSS 变量。

需求原文：`docs/DevKit-Pen完整页面设计任务书.md` 第 90–115 行（G01–G08）、第 503–530 行（第05节组件清单，含必需变体）。

## 必须做的事
1. 组件 API 与设计的对齐：把设计组件画板里的实际属性（尺寸、圆角、边框、颜色变量、字号、状态）与代码 CSS 对照，列出数值差异（例如按钮高度、输入框高度、圆角、状态色）。
2. 逐项功能验证（在浏览器或代码层）：
   - G04 stale 机制：`useToolRun` 是否覆盖了所有影响输出的字段？找 3–5 个工具确认“改输入后旧结果确实禁止复制/下载 + 出现重新执行”；
   - G01 跨工具传递：`useTransfer` 目标兼容性判断、替换确认、内存传递（刷新丢失是否合理）；
   - G03 剪贴板降级：非安全上下文/权限拒绝时是否自动降级提示（可用代码判断 + 手动构造失败路径）；
   - G02 文件反馈：FileDrop 的 accept/max-size/取消/失败文案；
   - G08 SVG 等危险内容：是否真的隔离（`iframe sandbox` / 不注入 DOM）、安全提示文案；
   - G05 示例数据：示例是否与输出自洽、是否有“示例结果”；是否存在默认填充输入导致“看起来算了”的假象；
   - G07 长内容：编辑器横向滚动/自动换行与查找是否实现。
3. 真实性红线检查（BRIEF 提到的第00节第33–45行 10 条规则的落地情况），例如：
   - 复制内容是否含行号/偏移；大整数是否丢精度；是否静默 trim/转换换行；“解码/解析/验签”文案区分；URL 是否放入输入或密钥（只允许工具 ID 与非敏感参数）。
4. 找出设计定义了但代码完全没有的交互/状态（例如某状态栏状态、某组件变体），以及代码有但设计没有的。
5. 代码缺陷每条给 `文件:行号`。

## 输出
写入 `/Users/hao/WebstormProjects/web_tools/audit/reports/r5-shared-and-components.md`，含“设计项 → 实现 → 结论”对照表，最后一行 `DONE: <绝对路径>`。
