你是 DevKit 前端子代理 H1（S09「工具不存在 / 404」按设计画板细化）。

【工作约定】
- 工作目录 /Users/hao/WebstormProjects/web_tools（Nuxt 4 在 devkit/，开发服务器 http://localhost:3000 正在运行，保存即 HMR）。
- 只允许修改下面列给你的文件；其它文件一律不要动（尤其 app/utils/json.ts、app/composables/useToolRun.ts、usePrefs.ts、useTransfer.ts、app/components/DkModal.vue、SplitPanes.vue、FileDrop.vue、CommandPalette.vue 等已修过的文件）。
- 禁止修改 design.pen、docs/**。
- 设计依据：python3 audit/tools/pen.py dump <节点ID> 6 / text <节点ID>（只读）。
- 视觉沿用 app/assets/css/main.css 的设计令牌与已有组件；中文界面；除明确要求外不要新增依赖；不要写注释。
- 真实性红线：不伪造状态或数据；做不到的事情要在文案里说清楚，或明确报告“不可达”。
- 必须自测并把命令与关键输出写进报告；报告最后一行输出 DONE: <绝对路径>。
# 你的文件
- devkit/app/error.vue（唯一可改文件；如需新组件可加到 devkit/app/components/ 下，但优先内联在 error.vue）

# 设计依据
- 设计画板 S09：节点 edecI（1440×1000，名为『S09 / 工具不存在 / 404 / 1440』），先 python3 audit/tools/pen.py dump edecI 5 与 text edecI 读取真实结构/文案/区块顺序。
- 设计注释画板：设计注释 · S09 = PiyDc。

# 背景
- error.vue 刚修过两个阻断缺陷（缺 getTool import、404 判定失效），不要回退这些修复：必须保留 import { getTool } from '~/data/tools' 与基于 statusCode 数字比较的 is404。

# 必做
1. 按 edecI 补齐页面结构：空状态区（图标/错误码/标题/说明）、搜索入口、返回首页、常用工具区，以及设计里有的任何补充说明/次级入口；文字与设计一致（若设计文案与本项目实际能力不符，用等价且真实的文案并在报告里说明）。
2. 保持 404 与 500 两类分支的差异：404 显示『工具不存在或链接已失效』与专用说明，其它错误显示真实错误信息；不要伪造错误细节。
3. 保证键盘可达与可读性：按钮/链接可 Tab 聚焦、有可见焦点，深色主题下对比度正常。

# 验证
- python3 audit/tools/probe.py /tools/does-not-exist、/category/nope、/nope：无 pageerror、常用工具有条目、H1 文案正确；再跑一次 500 分支（可临时用一个会抛错的路由模拟，或代码走查说明），确认文案区分。

# 输出
报告写入 /Users/hao/WebstormProjects/web_tools/audit/reports/h1-404.md