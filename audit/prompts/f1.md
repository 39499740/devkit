你是 DevKit 修复子代理 F1（密码学工具的真实性/假成功缺陷）。

【工作约定（必须遵守）】
- 工作目录：/Users/hao/WebstormProjects/web_tools（Nuxt 4 项目在 devkit/，开发服务器已在 http://localhost:3000 运行，改文件会自动 HMR）。
- 你只能修改下面明确列给你的文件；禁止修改任何其他源码文件，特别是共享文件：app/utils/json.ts、app/composables/*.ts、app/components/Dk*.vue、SplitPanes.vue、error.vue、FileDrop.vue、app/pages/ 下除你范围外的页面。
- 禁止修改 design.pen、docs/**。
- 上下文：完整审核报告在 audit/AUDIT-REPORT.md；你的问题在 audit/reports/ 下对应子报告里有精确的 文件:行号 与复现证据。先读这些证据再动手。
- 修复原则：(1) 真计算、不伪造；(2) 不静默改变用户数据（宁可报错或给出可见提示）；(3) 失败保留输入并给出具体修正方向；(4) 文案与事实一致（解码 != 验签 != 校验通过）；(5) 改动最小，不要顺手重构无关代码；不要写注释。
- 必须自测：改完用 node 复算（可把纯函数抽到 /tmp 下用 esbuild/tsx 跑）或 playwright 驱动 http://localhost:3000 真实页面验证；把命令与输出作为证据。
- 如果判断某个问题无法在不破坏其他行为的前提下修复，就不要硬改，在报告里说明原因。
- 完成后写报告到指定路径，最后一行输出：DONE: <绝对路径>
# 你的文件范围
- devkit/app/components/tools/t13-hmac.vue
- devkit/app/components/tools/t15-sm2.vue
- devkit/app/components/tools/t33-jwt.vue

# 要修的问题（详见 audit/reports/r2-tools-t01-t14.md、r3-tools-t15-t26.md、r4-tools-t27-t41.md）
1. 【T13】内置预设被标为 RFC 4231 Test Case 1，但实际数值不是 RFC 4231 向量（真 TC1：key = 20 个 0x0b 字节、data = Hi There、HMAC-SHA256 = b0344c61d8db38535ca8afceaf0bf12b881dc200c9833da726e9376c2e32cff7）。请把预置示例改成真实的 RFC 4231 向量（密钥用 Hex 0b0b...0b，与页面密钥输入格式一致），标签与数值必须自洽；若保留现有示例，则不得再声称是 RFC 4231。用 node 复算并把输出写进报告。
2. 【T15】SM2 公钥未做曲线点校验：格式合法但不在曲线上的点会被接受并显示加密成功，而该密文永不可解。请加入真实曲线点校验（验算 y^2 = x^3 + ax + b mod p），不在曲线上必须报错并保留输入（设计画板 XXObg 要求直接报错）。
3. 【T15】空明文解密时用 out==='' 且 hex.length===192 判定 C3 校验通过，而 sm-crypto 校验失败同样返回空串，导致伪造成功。请改为能区分「C3 校验失败」与「明文为空」的判定（例如自行校验 C3 = SM3(x2 || M || y2)），绝不能把失败当成功。
4. 【T33】被篡改签名的示例实际上没有被篡改（只改了 base64url 的填充位，解码字节完全相同），点击验签会显示签名验证通过，与设计和文案直接矛盾。请构造真正无效的示例（例如篡改 payload 段的一个字符，或改签名段的有效位），确保验签结论为不通过；同时修第二个问题：验签结论不随 token 变化清除（改 token 后旧的通过结论仍显示）。

# 输出
报告写入 /Users/hao/WebstormProjects/web_tools/audit/reports/fix-f1-crypto.md，逐条给出：问题 -> 改动（文件:行号）-> 验证命令与输出。最后一行 DONE: <绝对路径>