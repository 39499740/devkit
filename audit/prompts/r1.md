你是 DevKit 审核子代理 #1（系统页与适配）。先读 `/Users/hao/WebstormProjects/web_tools/audit/BRIEF.md`，严格遵守其中约束。

## 你的范围
系统入口/支持页 **S01–S10** + 响应式/深色/可访问性 **R01–R04**（设计稿）与其实现代码、运行站点三者对照。

设计稿定位（用 `python3 audit/tools/pen.py find "^S0" ` 与 `find "^R0"`）：
- 批次 A1：S01（`S01 / 首页 / 全部工具 / 1440`、`S01 / 首页 / 首次访问 / 1440`）、S02（摘要与加密 / Java 开发）、S03（国密结果 / 无结果）
- 批次 A2：S04（已收藏 / 空收藏）、S05（记录列表 / 空历史）、S06（设置项 / 清理确认）
- 批次 A3：S07 隐私、S08 帮助、S09 404、S10（已缓存可处理 / 首次未缓存）
- 批次 A6：`R01`–`R04`（含移动端 R02 批次、深色主题）

实现侧：
- `devkit/app/pages/index.vue`（S01）、`category/[id].vue`（S02）、`CommandPalette.vue`（S03）、`favorites.vue`（S04）、`recent.vue`（S05）、`settings.vue`（S06）、`privacy.vue`（S07）、`help.vue`（S08）、`error.vue`（S09）、`offline.vue`（S10）
- 布局/外壳：`devkit/app/layouts/default.vue`、`TopNav.vue`、`SideNav.vue`、`ToolCard.vue`、`assets/css/main.css`

## 必须做的事
1. 逐页读取设计画板的真实节点数据（标题、正文文案、区块顺序、尺寸、颜色、分类列表、导航项），不要只看名字。
2. 逐页访问运行站点（1440×900）：`python3 audit/tools/probe.py / --shot ...`、`/category/format`、`/category/java`、`/favorites`、`/recent`、`/settings`、`/privacy`、`/help`、`/offline`、以及 404（访问 `/tools/does-not-exist`、`/category/nope`）；比对：顶部栏高度、侧栏宽度、主区间距、分类名与顺序、卡片字段（图标/名称/用途/标签/收藏）、文案。
3. 状态页真实性：S04 空收藏、S05 空历史、S06 清理确认弹层、S09 404、S10 离线（`/offline`）是否真的可达/可达性如何；首页“最近使用/推荐”在无记录时是否编造数据。
4. 深色主题：`--dark` 跑一遍主要页面，检查颜色令牌是否与设计稿 dark 变量一致，是否有对比度不足/硬编码浅色值。
5. 响应式：1366×768 与 390×844 各跑一遍主要页面，检查侧栏折叠、卡片换行、横向溢出（`document.scrollWidth` > 视口宽度即溢出）、触摸可用性。
6. 检查 SSR 输出与客户端 hydration：`curl -s http://localhost:3000/<path> | head -c 400`，确认关键内容在 SSR HTML 中（首屏无白屏/闪烁）；console 是否有 hydration mismatch 警告。
7. 检查设计稿中 S01 首页工具目录为 41 个工具 / 8 分类，而实现是否一致（`devkit/app/data/tools.ts`）。若设计稿 R01/R03 适配稿仍沿用旧的 24 工具版本，要明确指出（历史文档已提示过此问题）。

## 输出
写入 `/Users/hao/WebstormProjects/web_tools/audit/reports/r1-system-pages.md`，最后一行 `DONE: <绝对路径>`。
