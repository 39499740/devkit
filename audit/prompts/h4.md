你是 DevKit 前端子代理 H4（T36 / T37 / T39 / T40 的设计细节补齐）。

【工作约定】
- 工作目录 /Users/hao/WebstormProjects/web_tools（Nuxt 4 在 devkit/，开发服务器 http://localhost:3000 正在运行，保存即 HMR）。
- 只允许修改下面列给你的文件；其它文件一律不要动（尤其 app/utils/json.ts、app/composables/useToolRun.ts、usePrefs.ts、useTransfer.ts、app/components/DkModal.vue、SplitPanes.vue、FileDrop.vue、CommandPalette.vue 等已修过的文件）。
- 禁止修改 design.pen、docs/**。
- 设计依据：python3 audit/tools/pen.py dump <节点ID> 6 / text <节点ID>（只读）。
- 视觉沿用 app/assets/css/main.css 的设计令牌与已有组件；中文界面；除明确要求外不要新增依赖；不要写注释。
- 真实性红线：不伪造状态或数据；做不到的事情要在文案里说清楚，或明确报告“不可达”。
- 必须自测并把命令与关键输出写进报告；报告最后一行输出 DONE: <绝对路径>。
# 你的文件
- devkit/app/components/tools/t36-http-status.vue
- devkit/app/components/tools/t37-file-digest.vue
- devkit/app/components/tools/t39-qrcode.vue
- devkit/app/components/tools/t40-text-encoding.vue

# 设计依据（先读再改）
- T36：cbuIP（常用列表）、i6SIKG（状态详情）、sSB8C（无搜索结果）。设计详情包含 方向/可缓存/幂等/RFC 参考/常见原因 等字段。
- T37：PKrN2（成功）、gKDNQ（部分失败）、b6Hjk（取消任务）、mYlnW（校验不匹配）。设计写明『导出摘要清单』为 **CSV**。
- T39：设计画板里的生成/识别状态；识别结果需要显示来源文件信息（文件名/大小）。
- T40：设计变体含『目标编码无法表示字符』。

# 必做
1. T36：按设计补详情字段（方向/是否可缓存/是否幂等/RFC 参考/常见原因），数据要准确（对照 RFC 9110/4918 等），不确定的字段宁可不加也不要编造。
2. T37：导出改为 CSV（正确转义引号/逗号/换行；列与设计一致），文件名与 MIME 正确；不要破坏现有的部分失败/取消/校验不匹配统计。
3. T39：识别结果展示来源文件信息（文件名与文件大小），生成侧不要伪造容量结论；保持二维码生成→识别回环可用。
4. T40：让『目标编码无法表示字符』成为**真实可达**的状态：增加 ISO-8859-1（Latin-1）作为真实可用的目标编码（用 charCode 实现，不使用 TextEncoder 不支持的能力），当输入含 > U+00FF 的字符时报出**具体位置与字符**并保留输入；GBK 等浏览器无法可靠编码的目标继续明确标注为不支持（不要假装支持）。

# 验证
- 每个工具 probe 无 error；T37 导出 CSV 用 node 解析验证（含含逗号/引号/换行的字段）；T40 用 emoji 或中文验证『无法表示』分支真实触发并给出位置；T36 抽查 200/301/404/424/503 的详情字段。

# 输出
报告写入 /Users/hao/WebstormProjects/web_tools/audit/reports/h4-tools-details.md