你是 DevKit 审核子代理 #2（工具 T01–T14）。先读 `/Users/hao/WebstormProjects/web_tools/audit/BRIEF.md`，严格遵守其中约束。

## 你的范围
工具 **T01–T14** 的设计还原 + 功能正确性 + 代码缺陷。
T01 JSON格式化、T02 JSON差异、T03 JSON/YAML、T04 CSV/JSON、T05 Base64、T06 URL编解码、T07 Unicode/字节、T08 文本去重、T09 命名风格、T10 文本差异、T11 正则测试、T12 MD5/SHA、T13 HMAC、T14 AES。

实现文件：`devkit/app/components/tools/t01-json-format.vue` … `t14-aes.vue`；公共能力 `devkit/app/utils/json.ts`、`devkit/app/utils/bytes.ts`、`devkit/app/composables/useToolRun.ts`。
需求原文：`docs/DevKit-Pen完整页面设计任务书.md` 第 120–245 行（T01–T14 小节，含“主画板提示词/示例要求/必画变体”）。
设计稿定位：`python3 audit/tools/pen.py find "^T0[1-9] /"`、`find "^T1[0-4] /"`。

## 必须做的事
1. 每个工具：读它的设计主画板 + 必画变体（用 `pen.py dump <id> 6` / `text <id>`），列出设计要求的控件、参数、按钮、状态栏字段、必画变体清单。
2. 每个工具：访问 `http://localhost:3000/tools/<slug>`（用 `python3 audit/tools/probe.py /tools/<slug>`），记录实际存在的控件与文案；给出**覆盖率判断**（设计有但代码没有 / 代码有但设计没有 / 文案不一致）。
3. 功能正确性：对每个工具用 node/python 复算它的核心逻辑并与实现里的算法对照，重点：
   - T01 大整数安全（19 位 ID 不能丢精度）、重复键提示、语法错误定位行号、缩进选项、树形视图；
   - T02 数组顺序语义、忽略键顺序开关；
   - T03 YAML 标签/非字符串键等不可无损映射的处理；
   - T04 引号/换行/前导零/列数不一致；
   - T05 二进制结果与非法字符处理；T06 无效百分号；T07 不完整转义/无效 UTF-8；
   - T08 去重是否 trim/排序会改变数据、保留空行；T09 无法分词的处理；T10 仅空白差异；
   - T11 正则超时与语法错误（ReDoS 防护是否真实存在）；
   - T12/T13 摘要与 HMAC 用公认测试向量（如 `echo -n abc | md5`）核对；T14 AES-GCM 认证失败与密钥长度错误的文案区分。
   能跑真实计算的，把命令与输出贴进报告。
4. 代码缺陷：读源码找真实 bug（边界、空输入、异常未捕获、结果复制不禁用、stale 逻辑漏字段、大数精度、编码/换行被静默修改、SSR 顶层访问 window、内存泄漏、watch 死循环等）。每条给 `文件:行号`。
5. 真实性红线：状态栏字符数/字节数/行数是否由真实数据推导；失败时是否保留输入；复制内容是否含行号等展示性内容。

## 输出
写入 `/Users/hao/WebstormProjects/web_tools/audit/reports/r2-tools-t01-t14.md`，含每工具一行的覆盖表，最后一行 `DONE: <绝对路径>`。
