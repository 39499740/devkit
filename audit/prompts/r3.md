你是 DevKit 审核子代理 #3（工具 T15–T26，密码学/时间/Java）。先读 `/Users/hao/WebstormProjects/web_tools/audit/BRIEF.md`，严格遵守其中约束。

## 你的范围
工具 **T15–T26** 的设计还原 + 功能正确性 + 代码缺陷。
T15 SM2、T16 SM3、T17 SM4、T18 时间戳、T19 UUID、T20 Cron、T21 日期差、T22 JSON→Java、T23 Java 转义、T24 Properties/YAML、T25 堆栈整理、T26 Maven/Gradle。

实现文件：`devkit/app/components/tools/t15-sm2.vue` … `t26-maven-gradle.vue`；依赖 `sm-crypto`、`js-yaml`。
需求原文：`docs/DevKit-Pen完整页面设计任务书.md` 第 246–353 行（T15–T26 小节）。
设计稿定位：`python3 audit/tools/pen.py find "^T1[5-9] /"`、`find "^T2[0-6] /"`。

## 必须做的事
1. 逐工具读设计主画板 + 必画变体（`pen.py dump <id> 6` / `text <id>`），列出设计要求与实际实现的差异。
2. 密码学必须用**权威测试向量**核对，不能只看代码“看起来对”：
   - SM3：`sm3('abc')`，标准值 `66c7f0f462eeedd9d1f2d46bdc10e4e24167c4875cf2f7a2297da02b8f4ba8e0`；
   - SM4：标准 ECB 向量（key/plaintext `0123456789abcdeffedcba9876543210` → `681edf34d206965e86b3e94f536e4246`），并核对 CBC/IV/填充处理与 UTF-8 字节数；
   - SM2：加解密、签名、验签、公钥/私钥格式错误处理；注意“解码成功 ≠ 验签成功 ≠ 校验通过”的文案是否严格区分；
   - T12/T13 若相关也顺带核对（可选）。
   用 `node -e`（`require('sm-crypto')`，cwd 用 `devkit/`）真实跑，把命令与输出贴进报告。若实现有偏差，判断是实现 bug 还是设计缺陷。
3. 非密码学部分：
   - T18 时间戳：秒/毫秒判定、时区、夏令时、无效日期；
   - T19 UUID：v4 版本/变体位、批量、解析（时间戳/variant）、非法输入；
   - T20 Cron：五字段与 Quartz 六/七字段方言、`?`/`L`/`W`/`#` 支持、下次执行时间计算是否正确（用真实日期核对）、无后续执行；
   - T21 日期差：结束早于开始、跨时区、DST；
   - T22 JSON→Java：嵌套、数组、类型推断、大整数、非法类名、record 输出；生成代码是否可编译（可初判）；
   - T23 Java 转义：完整转义表（\uXXXX、八进制、非法转义、反向还原）；
   - T24 Properties/YAML：父子键冲突、Unicode 转义、回转；
   - T25 堆栈整理：cause 链、`... N more`、混入普通日志、无法识别；
   - T26 Maven/Gradle：无 version、父 POM 继承、`${}` 表达式、需要手动映射；声明转换是否正确。
4. 检查“伪造成功状态”：未实现/未校验的功能是否仍显示成功或可复制。
5. 代码缺陷：每条给 `文件:行号` 与可复现命令。

## 输出
写入 `/Users/hao/WebstormProjects/web_tools/audit/reports/r3-tools-t15-t26.md`，含每工具一行的覆盖表，最后一行 `DONE: <绝对路径>`。
