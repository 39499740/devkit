你是 DevKit 审核子代理 #4（工具 T27–T41，Web/Vue/接口/文件）。先读 `/Users/hao/WebstormProjects/web_tools/audit/BRIEF.md`，严格遵守其中约束。

## 你的范围
工具 **T27–T41** 的设计还原 + 功能正确性 + 代码缺陷。
T27 JSON→TS、T28 Vue SFC、T29 CSS 单位、T30 颜色与对比度、T31 SVG 预览优化、T32 HTML/CSS/JS 格式化、T33 JWT、T34 URL 参数、T35 curl/fetch/axios、T36 HTTP 状态码、T37 文件摘要、T38 图片压缩、T39 二维码、T40 文本编码/换行、T41 文件/Base64。

实现文件：`devkit/app/components/tools/t27-json2ts.vue` … `t41-file-base64.vue`；相关依赖 `qrcode`、`jsqr`、`spark-md5`。
需求原文：`docs/DevKit-Pen完整页面设计任务书.md` 第 354–488 行（T27–T41 小节）。
设计稿定位：`python3 audit/tools/pen.py find "^T2[7-9] /"`、`find "^T3[0-9] /"`、`find "^T4[01] /"`。

## 必须做的事
1. 逐工具读设计主画板 + 必画变体，列出设计要求 vs 实现的覆盖差异（控件、参数、状态、文案）。
2. 功能正确性（要真跑，不要只读代码）：
   - T27 类型推断：null/联合类型/空数组/混合数组/嵌套/非法标识符；
   - T28 SFC 模板：props/emits、组件名合法性校验；
   - T29 CSS 单位：px/rem/em/pt/vw/vh/% 换算正确性、基准字号非法；
   - T30 颜色：hex/rgb/hsl 互转、带 alpha、对比度公式（WCAG 相对亮度与比值）、低对比结论；
   - T31 SVG：是否真的阻止脚本/事件属性/外链（XSS 防护）；优化是否改变视觉；
   - T32 格式化：HTML/CSS/JS 三种模式，是否有真实解析器（还是正则？正则即高危）；语法错误提示；
   - T33 JWT：base64url 解码、过期判断、HS256/RS256 验签是否真实（用真实密钥生成 token 验证“签名不通过/通过/过期但有效”三种文案是否正确区分）；**JWT 解码成功绝不能显示签名有效**；
   - T34 URL 参数编辑：重复参数、空值、编码；
   - T35 curl 转换：不支持表达式、multipart、引号/转义；
   - T36 状态码：数据完整性（对照 RFC 状态码集合，列出缺失/错误）；
   - T37 文件摘要：分片读取、进度、批量比对；
   - T38 图片压缩：canvas 重编码、格式转换、EXIF/透明度损失是否提示；
   - T39 二维码：生成（对照规范/解码回环验证）、识别 jsQR；
   - T40 编码/换行转换：GBK 等编码（浏览器 TextDecoder 支持范围）、CRLF/LF、BOM；
   - T41 文件/Base64：大文件内存、DataURL 前缀处理、错误处理。
   可用 node/python 复算的都要复算，命令与输出贴进报告。
3. 检查文件类工具（T37–T41）是否具备设计中要求的文件名/大小/移除/进度/取消/失败反馈（G02），是否出现“上传中”等违规文案。
4. 代码缺陷：每条给 `文件:行号` + 复现步骤。

## 输出
写入 `/Users/hao/WebstormProjects/web_tools/audit/reports/r4-tools-t27-t41.md`，含每工具一行的覆盖表，最后一行 `DONE: <绝对路径>`。
